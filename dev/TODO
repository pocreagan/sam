scrape RACC
diff RACC from prev
    if necessary
        for each change
            update bucket -> serving size lookup


BUGS


FEATURES
add dyn food id search
add splash screen with build date center bottom
stop the rendering shit at the beginning
save output in a dir on ~/Desktop